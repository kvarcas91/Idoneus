<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Idoneus.Resources.Styles"
                     xmlns:converter="clr-namespace:Idoneus.Converters"
                     xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                     xmlns:converters="clr-namespace:Idoneus.Converters">

    <Style  x:Key="ProjectListViewSnippet" TargetType="{x:Type ListView}" BasedOn="{StaticResource MaterialDesignListView}">

        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid VerticalAlignment="Center"
                          Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">

                            <!-- Priority Color Indicator -->
                            <Border Grid.Column="0"
                                Background="{Binding Priority, Converter={converters:PriorityToColorConverter}}" 
                                Width="2" 
                                Height="10"
                                Margin="5 3 5 3"/>

                            <!-- Project summary -->
                            <TextBlock Grid.Column="0"
                                   Text="{Binding Content}"
                                   TextTrimming="WordEllipsis"/>
                        </StackPanel>
                        
                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>

                                <!-- completed tasks / Total tasks -->
                                <ColumnDefinition Width="Auto"/>

                                <!-- Progress bar -->
                                <ColumnDefinition Width="*"/>

                                <!-- Progress percentage -->
                                <ColumnDefinition Width="60"/>

                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                   Margin="0 0 10 0"
                                       TextAlignment="Left">
                            <Run Text="{Binding CompletedTaskCount}"/>
                            <Run Text="/"/>
                            <Run Text="{Binding Tasks.Count, Mode=OneWay}"/>
                            </TextBlock>

                            <ProgressBar Grid.Column="1"
                                     Value="{Binding Progress}"
                                         Height="1"
                                     Style="{StaticResource MaterialDesignLinearProgressBar}"
                                      Foreground="{Binding Priority, Converter={converters:PriorityToColorConverter}}"/>

                            <TextBlock Grid.Column="2"
                                   Margin="10 0 10 0"
                                       TextAlignment="Right">
                            <Run Text="{Binding Progress}"/>
                            <Run Text="%"/>
                            </TextBlock>

                        </Grid>

                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Upcoming tasks view from dashboard -->
    <Style x:Key="UpcomingTasksListView" TargetType="{x:Type ListView}" BasedOn="{StaticResource MaterialDesignListView}">
        
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid VerticalAlignment="Center"
                          Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding DueDate, Converter={converter:DateTimeToStringConverter}, ConverterParameter=0}"
                                       FontSize="10"
                                       Style="{StaticResource MaterialDesignCaptionTextBlock}"/>

                        <!-- Item summary -->
                        <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                   Text="{Binding Content}"
                                   TextTrimming="WordEllipsis"/>

                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <!-- Disable onMouseOver and default style -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Today's task list view from dashboard -->
    <Style x:Key="TodayTaskListView" TargetType="{x:Type ListView}" BasedOn="{StaticResource MaterialDesignListView}">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>

                    <Border Margin="5">

                        <CheckBox Grid.Row="0"
                                  Padding="3 0 3 0"
                                  IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                  Command="{Binding ElementName=TaskListView, Path=DataContext.SelectTaskCommand}"
                                  VerticalAlignment="Center"
                                  CommandParameter="{Binding}">
                            <TextBlock  Grid.Column="1"
                            Text="{Binding Content}"
                                    TextAlignment="Justify"
                                    TextWrapping="Wrap"/>
                        </CheckBox>

                    </Border>

                </DataTemplate>
            </Setter.Value>
        </Setter>

        <!-- Disable onMouseOver and default style -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Today's task list view from dashboard -->
    <Style x:Key="TaskTemplateListView" TargetType="{x:Type ListView}" BasedOn="{StaticResource MaterialDesignListView}">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>

                    <Grid Margin="5">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock  Grid.Column="0"
                                    VerticalAlignment="Center"
                                    Text="{Binding Content}"
                                    TextAlignment="Justify"
                                    TextWrapping="Wrap"/>

                        <ToggleButton Grid.Column="1"
                            Style="{DynamicResource MaterialDesignFlatToggleButton}"
                                      IsChecked="{Binding IsActive}"
                                       Command="{Binding ElementName=TaskTemplateList, Path=DataContext.SelectTemplateCommand}"
                                      VerticalAlignment="Center"
                                      Width="30"
                                      Height="30"
                                CommandParameter="{Binding}">
                            <materialDesign:PackIcon Kind="{Binding IsActive, Converter={converter:BoolToIconTaskTemplateIconConverter}}"
                                                     VerticalAlignment="Stretch"
                                                     HorizontalAlignment="Stretch"
                                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                       Width="24"
                                      Height="24"/>
                        </ToggleButton>
                                      
                        
                        <Button Grid.Column="2"
                                VerticalAlignment="Center"
                                HorizontalContentAlignment="Right"
                            Style="{StaticResource MaterialDesignFlatButton}"
                                Command="{Binding ElementName=TaskTemplateList, Path=DataContext.DeleteTemplateCommand}"
                                CommandParameter="{Binding}">
                            <materialDesign:PackIcon Kind="Delete"/>
                        </Button>

                       
                        
                       

                    </Grid>

                </DataTemplate>
            </Setter.Value>
        </Setter>

         <!--Disable onMouseOver and default style--> 
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Project sidebar list style -->
    <Style x:Key="ProjectListView" TargetType="{x:Type ListView}" BasedOn="{StaticResource MaterialDesignListView}">

        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>

                    <Grid x:Name="container"
                            Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.InputBindings>
                            <MouseBinding Gesture="LeftClick" 
                                    Command="{Binding ElementName=projectListView, Path=DataContext.SelectProjectCommand}"
                                    CommandParameter="{Binding}"/>
                        </Grid.InputBindings>

                        <!-- Priority bar -->
                        <Border Background="{Binding Priority, Converter={converter:PriorityToColorConverter}}" 
                                Width="4" 
                                HorizontalAlignment="Left"/>

                        <!-- Progress -->
                        <Border Grid.Column="0" 
                        Padding="8 15">

                            <Grid >

                                <Border Background="Transparent"
                                BorderBrush="{Binding Priority, Converter={converter:PriorityToColorConverter}}"
                                BorderThickness="1.5"
                                Margin="1"
                                Opacity="0.5"
                                CornerRadius="{Binding ActualHeight, RelativeSource={RelativeSource self}}"
                                Width="{Binding ActualHeight, RelativeSource={RelativeSource self}}">

                                    <TextBlock  
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                FontFamily="Arial"
                                FontWeight="Bold"
                                    Foreground="{DynamicResource SecondaryHueMidBrush}" >
                                    <Run Text="{Binding Progress}"/>
                                    <Run Text="%"/>
                                    </TextBlock>
                                </Border>

                                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            Height="40"
                            Width="40"
                            Value="99.9"
                            Foreground="LightGray"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>

                                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            Height="40"
                            Width="40"
                            Foreground="{Binding Priority, Converter={converter:PriorityToColorConverter}}"
                            Value="{Binding Progress, Converter={converter:ProgressConverter}}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>



                            </Grid>

                        </Border>

                        <!-- Main Content -->
                        <Border Padding="0 0 8 0"  Grid.Column="1">

                            <StackPanel VerticalAlignment="Center" >

                                <!-- Header -->
                                <TextBlock Text="{Binding Header}"
                                    Padding="0 0 0 2"
                                         
                                    Foreground="{DynamicResource PrimaryHueMidBrush}"
                                    TextTrimming="CharacterEllipsis"/>

                                <!-- Content -->
                                <TextBlock Text="{Binding Content}"
                                    Padding="0 2 0 0"
                                    Foreground="Gray"
                                    ToolTip="{Binding Content}"
                                    TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>

                        <Separator Grid.Row="1" 
                            Grid.ColumnSpan="2"
                            Margin="10 0 10 0"/>
                    </Grid>
                        
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <!-- Disable onMouseOver and default style -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>